(defwidget apps []
;    (box :class "revealer"
;         :orientation "h"
;         :space-evenly "false"
;         (label :class "greater"
;                :text ">")
    (box :class "apps"
             :orientation "h"
             :spacing "11"
             (librewolf)
             (steam)
             (discord)
             (signal-desktop)
             (foot-btop)
             (foot-nvim)
             (pcmanfm)
             (ungoogled-chromium)
             (prismlauncher)))

